# Prereqs
# sudo apt-get install software-properties-common
# sudo apt-add-repository ppa:ansible/ansible
# sudo apt-get update
# sudo apt-get install ansible

# Execution
# sudo ansible-pull -U https://github.com/suderio/dotfiles.git

---
- include_playbook: .playbooks/basic.yml
- include_playbook: .playbooks/gnome.yml
- include_playbook: .playbooks/apps.yml


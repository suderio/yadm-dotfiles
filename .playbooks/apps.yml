- hosts: localhost
  become: true
  tasks:
  - name: Install snaps
    command: snap install {{item}}
    with_items:
      - vlc
      - gimp
      - libreoffice
      - telegram-desktop
      - spotify
      - cool-retro-term
  - name: Install classic snaps
    command: snap install {{item}} --classic
      - skype


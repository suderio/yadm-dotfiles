- hosts: localhost
  become: true
  tasks:
  - name: Install packages
    package: name={{item}}
    with_items:
      - htop
      - yadm
      - curl
      - wget
      - vim
      - vim-doc
      - vim-scripts
      - xclip
      - patch
      - less
      - git-doc
      - git-email
      - tlp
      - tlp-rdw
      - docker.io
      - docker-doc
      - gcc 
      - make 
      - perl 
      - automake 
      - flex 
      - bison 
      - gcc-doc 
      - make-doc 
      - glibc-doc 
      - autoconf-doc 
      - bison-doc 
      - build-essential 
      - flex-doc 
      - m4-doc
        # - ubuntu-restricted-extras tem q desabilitar a tela de autorização
        # yadm clone git@github.com:suderio/dotfiles.git

